# Minimal dependency set for the drift/performance metrics exporter -
# doesn't need FastAPI, mlflow, or scikit-learn, just data access + stats.
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
python-dotenv==1.0.1
prometheus-client==0.21.0
