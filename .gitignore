# Python
.venv/
__pycache__/
*.pyc

# Environment / secrets
.env

# MLflow local artifact/tracking store (not project source; regenerated by running the pipeline)
mlruns/
mlartifacts/
mlflow.db

# Claude Code local session config (machine-local tool permissions, not project source)
.claude/

# DVC cache
/data/raw/*
/data/processed/*
!/data/raw/.gitkeep
!/data/processed/.gitkeep

# OS
.DS_Store
